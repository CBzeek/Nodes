# *Aptos DevNet*
#
#
*Prepare for install commands:*
`
sudo apt install screen -y && screen -S aptos
`
#
Prepare for install commands:
`
sudo apt install screen -y && screen -S aptos
`
#
Prepare for install commands:
`
sudo apt install screen -y && screen -S aptos
`
#
Prepare for install commands:
`
sudo apt install screen -y && screen -S aptos
`
#
Prepare for install commands:
`
sudo apt install screen -y && screen -S aptos
`
